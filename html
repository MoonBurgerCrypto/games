<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>My Game Hub</title>
<style>
  body {
    margin: 0;
    font-family: Arial, sans-serif;
    background: #101820;
    color: #fff;
  }
  header {
    background: #182533;
    padding: 15px 20px;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }
  h1 { margin: 0; font-size: 20px; }
  .grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 15px;
    padding: 20px;
  }
  .card {
    background: #1e2b3a;
    padding: 15px;
    border-radius: 10px;
    text-align: center;
    cursor: pointer;
    transition: transform 0.2s;
  }
  .card:hover {
    transform: scale(1.03);
    background: #26384c;
  }
  iframe {
    width: 100%;
    height: 80vh;
    border: none;
    display: none;
  }
  .back {
    display: none;
    padding: 10px 20px;
    margin: 10px;
    background: #00d1ff;
    color: #000;
    font-weight: bold;
    border: none;
    border-radius: 8px;
    cursor: pointer;
  }
</style>
</head>
<body>

<header>
  <h1>🎮 My Game Hub</h1>
</header>

<button class="back" id="backBtn" onclick="closeGame()">⬅ Back to Games</button>

<iframe id="gameFrame" allowfullscreen></iframe>

<div class="grid" id="gameGrid"></div>

<script>
  // 🎯 Add your games here (only use sites that allow embedding)
  const games = [
    { title: "Drift Boss", url: "https://www.mathplayground.com/pg_drift_boss.html" },
    { title: "Slope", url: "https://www.slopegame.io/" },
    { title: "Moto X3M", url: "https://www.crazygames.com/game/moto-x3m" },
    { title: "1v1.LOL", url: "https://1v1.lol/" },
    { title: "Basket Random", url: "https://www.crazygames.com/game/basket-random" },
    { title: "Retro Bowl", url: "https://www.crazygames.com/game/retro-bowl" }
  ];

  const grid = document.getElementById("gameGrid");
  const frame = document.getElementById("gameFrame");
  const backBtn = document.getElementById("backBtn");

  // Generate game cards
  games.forEach(game => {
    const card = document.createElement("div");
    card.className = "card";
    card.innerHTML = `<h3>${game.title}</h3>`;
    card.onclick = () => openGame(game.url);
    grid.appendChild(card);
  });

  // Open game inside iframe
  function openGame(url) {
    frame.src = url;
    frame.style.display = "block";
    grid.style.display = "none";
    backBtn.style.display = "inline-block";
    window.scrollTo({ top: 0, behavior: "smooth" });
  }

  // Go back to game list
  function closeGame() {
    frame.src = "";
    frame.style.display = "none";
    grid.style.display = "grid";
    backBtn.style.display = "none";
  }
</script>

</body>
</html>
